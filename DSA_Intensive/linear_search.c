#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int num = 0;
    printf("%d", num);
    printf("%p", &num);

    int number = 17;
    printf("%d", number);
    printf("%p", &number);
    return 0;
}